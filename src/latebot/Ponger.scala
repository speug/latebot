/*

package latebot

import akka.actor.{ Actor, ActorLogging, ActorRef, ActorSystem, Props }

class Ponger(main: ActorRef) extends Actor {

  def receive = {
    case Message => main ! this.pong()
  }
}
*/
